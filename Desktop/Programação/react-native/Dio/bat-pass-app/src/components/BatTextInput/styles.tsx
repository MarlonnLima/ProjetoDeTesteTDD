import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: "#e5bf3c",
    width: "100%",
    color: "#000",
    fontSize: 20,
    height: 40,
    padding: 10,
    borderRadius: 10,
    textAlign: "center"
  }
});